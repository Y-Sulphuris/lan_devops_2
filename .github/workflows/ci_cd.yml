name: Version update pipeline

on:
  pull_request:
    types: [closed]
    branches:
    - release
env:
  NAME_VERSION_FILE: version
jobs:
  validate-branch:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo "step 1"
      - name: step 2
        run: |
          echo "step 2"
          echo "step 2 - second command"
